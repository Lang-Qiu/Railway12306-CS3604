id: REQ-02-08
name: 注册-用户名输入
functional_description: 用户名设置与校验。
ui_description: |
  必填项。
  失去焦点时显示校验结果（绿色勾或红色错误文案）。
input_parameters:
  - name: username
    type: string
output_parameters: null
preconditions: null
postconditions:
  - 验证通过显示绿勾
business_rules:
  - 6-30位字母、数字或下划线
  - 必须以字母开头
  - 唯一性校验（调用后端）
exceptions:
  - 格式错误提示
  - 用户名已存在提示
scenarios:
  - name: 输入已存在用户名
    steps:
      - action: 输入"existing_user"，失焦
      - expectation: 提示"该用户名已经占用"。
