## 交付内容
- 提供站点所有已实现页面的路由与文件路径、主要功能、导航关系、访问权限、响应式适配现状
- 标注未实现但存在导航入口的页面与缺失的特殊页面（404）

## 后续完善建议
1. 增加 404 兜底路由
- 在 `frontend/src/App.tsx` 添加 `path="*"` 对应的 `NotFound` 页面
- 提供简单引导返回首页或常用功能

2. 补齐无效导航项与对应页面
- `MainNavigation` 中的 `group-service/member-service/...` 暂无路由；移除或新增占位页并挂载路由
- `TopNavigation` 的 `我的12306` 子项应仅指向已实现页面（`/orders`、`/passengers`），其余项新增占位或隐藏

3. 基于 Token 的访问控制
- 在前端增加轻量 `RequireAuth` 组件，保护 `订单提交`、`订单历史`、`乘车人管理` 等关键页面
- 后端新增认证中间件校验 `Authorization`，在 `/api/orders`、`/api/passengers` 等接口应用

4. 一致的页面跳转与状态
- 统一登录后跳转策略（例如统一跳转首页或“我的12306”）
- 修复未实现路由引用（如 `/personal-info`）

5. 移动端适配优化
- 基于现有 `@media` 媒体查询，补齐常见断点与关键交互组件在窄屏下的布局（表格、表单、导航）

如认可该计划，我将按以上顺序进行最小改动、逐项落地并提交。